import { createTheme } from 'react-native-whirlwind'

/** Create the default theme */
export default createTheme({
  colors: {
    // Uncomment one or more of the following lines and see what happens
    // primary: 'red',
    // paper: 'yellow'
  }
})
